## default-user

Creates a user named `debian` with password `debian`. Please change the password
after login, SSH allows password-login and if you machine is publicly accessible
it will be compromised in no time.
